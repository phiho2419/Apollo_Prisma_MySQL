var fs = require("fs")

const countUserLogin = (userId) => {
    for (let i = 0; i < arr.length; i++) {
        if(userId != arr[i]){
            arr.push(userId)
        }
    }
    
}

module.exports = {
    countUserLogin
}